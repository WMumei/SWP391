@model List<QuotationRequest>
<form method="post">
    <div>
        <div>
            <h2>View Quotation Application</h2>
            <hr />
        </div>
        @if (Model.Count > 0)
        {
            <table class="table table-bordered table-striped">
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>Name</th>
                        <th>Status</th>
                        <th>CreatedAt</th>
                        <th>Labor Price</th>
                        <th>Total Price</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var obj in Model.OrderBy(u => u.CreatedAt)) 
                    {
                        @if (obj.JewelryId == Convert.ToInt64(Context.Request.Query["jId"]))
                        {
                            <tr>
                                <td>@obj.Id</td>
                                <td>@obj.Name</td>
                                <td>@obj.Status</td>
                                <td>@obj.CreatedAt</td>
                                <td>@obj.LaborPrice</td>
                                <td>@obj.TotalPrice</td>
                                <td><a asp-controller="QuotationRequest" asp-action="Details" asp-route-jId="@obj.JewelryId" class="btn btn-primary btn-sm">View</a></td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        }
        else
        {
            <p>No quotation requests found for the selected jewelry.</p>
        }
    </div>

    <div class="col-6 col-md-3">
        <a asp-controller="ProductionRequest" asp-action="Index" class="btn btn-outline-primary form-control">
            Back 
        </a>
    </div>

    <!-- <div asp-validation-summary="All"></div> -->
</form>